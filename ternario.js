const idadeMinima = 18;
const idadeCliente = 16;

if (idadeCliente >= idadeMinima) {
  console.log("Cerveja");
} else {
  console.log("Beba agua");
}

// Operador ternário:

const idadeMinima1 = 18;
const idadeCliente1 = 16;

// condição                     //true      //false
console.log(idadeCliente1 >= idadeMinima1 ? "Cerveja" : "Beba agua");
// >= operador comparação // ?operador de separação  // : resultados

// Utilizando 3 operadores em uma única linha.
